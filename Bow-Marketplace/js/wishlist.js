document.addEventListener("DOMContentLoaded",async()=>{const box=document.getElementById("wishlistGrid");try{await S.fetchItems();const items=S.wishlist.map(id=>S.itemById(id)).filter(Boolean);box.innerHTML=items.length?items.map(i=>cardHtml(i)+`<button class="remove-wide" data-remove-wish="${U.escape(i.id)}">Remove from wishlist</button>`).join(""):"<section class='empty-state'><h2>Your wishlist is empty</h2><p>Save items from the marketplace to see them here.</p></section>";}catch(e){box.innerHTML="<section class='empty-state'><h2>Wishlist unavailable</h2></section>";}});