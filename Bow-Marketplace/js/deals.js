document.addEventListener("DOMContentLoaded",async()=>{const box=document.getElementById("dealsGrid");try{await S.fetchItems();const deals=S.items.filter(x=>!isSold(x)&&(x.discount||x.salePrice<x.itemValue));box.innerHTML=deals.length?deals.map(cardHtml).join(""):"<section class='empty-state'><h2>No deals right now</h2></section>";}catch(e){box.innerHTML="<section class='empty-state'><h2>Deals unavailable</h2></section>";}});